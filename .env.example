# Active client configuration
# Set this to "hortman" or "milena" to switch between clients
ACTIVE_CLIENT=hortman

# DialogGauge API session (optional - will auto-login via Playwright if not set)
# DG_SESSION=your_session_cookie_here

# Google Sheets API credentials path (if using sheets integration)
# GOOGLE_CREDENTIALS_PATH=config/credentials.json
